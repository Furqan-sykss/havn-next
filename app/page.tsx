import HomeExperience from "@/components/HomeExperience";

/** Server component: the page is composed here, the browser work is isolated below. */
export default function Page() {
  return <HomeExperience />;
}
